## compile

```
$npm install


### test
vite.config.tsのIS_DEVをfalseにして。app.tsが起動する。

```
$npm run dev
```





### build
vite.config.tsのIS_DEVをfalseにして。distに何かができる。

```
$npm run build
```
